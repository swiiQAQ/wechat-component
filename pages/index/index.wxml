<!--index.wxml-->
<view class="index">
  <view class="index-hd">
    <image src="../../images/logo.png"></image>
    <text class="index-desc">以下将展示自定义组件，组件样式仅供参考，开发者可根据自身需求自定义组件样式</text> 
  </view>
  <view class="index-ct">
    <view class="list">
      <view class="list-item">
         <navigator wx:for="{{list}}" url="../components/componentRef/{{item.pages}}/{{item.pages}}">
          <text>{{item.name}}</text>        
        </navigator> 
        <!-- <view wx:for="{{list}}">
          <text>{{item.name}}</text>        
            <score max="{{score.max}}" score="{{score.score}}"></score>  
        </view> -->
      </view>
    </view>
  </view>
</view>
